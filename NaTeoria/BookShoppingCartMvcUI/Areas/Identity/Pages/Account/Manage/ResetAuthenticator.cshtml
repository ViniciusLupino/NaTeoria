@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Redefinir chave do autenticador";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3 class="text-center mb-4" style="color: #2C3E50;">@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert" style="background-color: #759B6A; color: white;">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Se você redefinir sua chave do autenticador, seu aplicativo autenticador não funcionará até que você o reconfigure.</strong>
    </p>
    <p>
        Esse processo desativa o 2FA até que você verifique seu aplicativo autenticador.
        Se não completar a configuração do aplicativo autenticador, você poderá perder o acesso à sua conta.
    </p>
</div>

<div class="text-center">
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit" style="background-color: #2C3E50; color: white; border: none;">Redefinir chave do autenticador</button>
    </form>
</div>
