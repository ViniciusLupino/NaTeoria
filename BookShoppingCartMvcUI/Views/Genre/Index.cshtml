@model IEnumerable<Genero>
@{
    Layout = "_AdminLayout";
}

<h2>Generos</h2>

<a asp-action="AddGenre" asp-controller="Genre" class="btn btn-primary">Add More</a>

<table class="table table-striped my-2">
    <tr>
        <th>genero</th>
        <th>Action</th>
    </tr>

    @foreach(var genero in Model)
    {
        <tr>
            <td>@genero.GeneroName</td>
            <td>
                <a asp-action="UpdateGenre" asp-controller="Genre" asp-route-id="@genero.Id" class="btn btn-success">Edit</a>
                <a asp-action="DeleteGenre" asp-controller="Genre" asp-route-id="@genero.Id" class="btn btn-danger" onclick="return window.confirm('Are you sure?')">Delete</a>
            </td>
        </tr>
    }
</table>

